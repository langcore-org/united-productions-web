/**
 * アプリケーション全体で使用される定数
 *
 * ハードコードされた値を一元管理し、変更時の修正箇所を最小化する。
 */

/** インメモリキャッシュのTTL（ミリ秒） */
export const CACHE_TTL_MS = 60 * 1000;

/** テキストエリアの最大高さ（px） */
export const TEXTAREA_MAX_HEIGHT_PX = 200;

/** ファイルアップロードのサイズ上限（MB） */
export const MAX_FILE_SIZE_MB = 10;
