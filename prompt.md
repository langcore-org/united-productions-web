## new project screen
- add new button
  - as is : with out icon
  - to be : with icon in left side( in the same line)
- button color
  - as is : gray
  - to be : blue



## new project screen
- button color
  - as is : gray
  - to be : blue



## files tab
- keep files list
  - as is : do not keep, when user switch to another session or reload the window. file list is empty
  - to be : keep file list in the sqlite database.

## keep running session
- keep running session
  - as is : when user switch to another session or reload the window. sessino is stopped. this is not good. because agent response is so slow.
  - to be : run agent in the background. 

- displya running session on chat list panel
  - as is : do not display
  - to be : display runngng session on chat list panel with animated icon


## file selector
- file selector
  - as is : 現在、 @ markで fileを loadする時に、messageの外で、選ばれている状態になる。
  - to be : (@generate_infography) を読み込んで、 要約して みたいに、messageの内で選ばれている状態になる。
  - 目的 : userが loadした fileがどのような目的で使用するのかを明確にすることができる。
- directory selector
  - as is : fileだけ選択できる
  - to be : directoryも選択できる, この場合、 directoryの中の全ての fileを対象とする。


現在、 @ markで fileを loadする時に、
  messageの外で、選ばれている状態になる。
  [Image #1]

  to bo :
  

## files preview
- files preview
  - as is : simple text loader
  - to be : markdown preview (with link and with image)a